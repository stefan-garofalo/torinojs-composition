# Backlog Provider

Selected backlog provider: GitHub Projects and Issues (`github`).

Keep this provider pinned unless the user explicitly changes it. When requirements are ready for backlog writing, use `write-backlog` with the matching provider payload guidance.
